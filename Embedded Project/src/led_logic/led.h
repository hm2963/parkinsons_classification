#ifndef LED_H
#define LED_H

#include <Arduino.h>

void initLED(); //replace this with your prefered init function name
void handleLEDDK(float intensity); //replace this with your prefered init function name and arguments
void handleLEDTM(float intensity);
void handleLEDN(float intensity);

#endif
